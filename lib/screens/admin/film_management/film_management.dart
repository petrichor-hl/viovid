import 'package:flutter/material.dart';

class FilmManagementScreen extends StatelessWidget {
  const FilmManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Film Management Screen',
      ),
    );
  }
}
