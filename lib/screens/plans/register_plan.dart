import 'package:flutter/material.dart';

class RegisterPlan extends StatelessWidget {
  const RegisterPlan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Plan List Screen',
          style: TextStyle(color: Colors.white),
        ),
      ),
    );
  }
}
